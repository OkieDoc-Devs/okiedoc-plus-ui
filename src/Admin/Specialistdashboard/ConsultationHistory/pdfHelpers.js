// Temporary placeholder functions for PDF/email actions
export const downloadTreatmentPlanPDF = (consultation) => {
  console.log("Download Treatment Plan PDF:", consultation);
};

export const downloadPrescriptionPDF = (consultation) => {
  console.log("Download Prescription PDF:", consultation);
};

export const downloadLabRequestPDF = (consultation) => {
  console.log("Download Lab Request PDF:", consultation);
};

export const downloadMedicalCertificatePDF = (consultation) => {
  console.log("Download Medical Certificate PDF:", consultation);
};

export const downloadMasterPDF = (consultation) => {
  console.log("Download Master PDF:", consultation);
};

export const sendToEmail = (consultation) => {
  console.log("Send consultation to email:", consultation);
};

